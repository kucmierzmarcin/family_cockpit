# Import wydarzeń z AI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dodać ekran "Importuj z AI", który na podstawie promptu i/albo pliku (zdjęcie/PDF) rozpoznaje przez Claude API listę wydarzeń kalendarza, pokazuje je do akceptacji i zapisuje wybrane do istniejących tabel `events`/`event_members`.

**Architecture:** Nowa Edge Function `import-ai` tylko rozpoznaje (woła Claude z wymuszonym structured output) i niczego nie zapisuje. Klient pokazuje podgląd i po akceptacji zapisuje bezpośrednio przez istniejącą ścieżkę `supabase-js` (te same reguły RLS co ręczne dodawanie wydarzenia). Cała logika budowania zapytania do Claude i walidacji jego odpowiedzi to czyste funkcje bez Deno/sieci - testowalne zwykłym vitestem.

**Tech Stack:** React + TypeScript (Vite), Supabase (Postgres + Auth + Edge Functions/Deno), Claude Messages API (tool-use), vitest.

**Spec:** `docs/superpowers/specs/2026-09-07-import-ai-design.md`

## Global Constraints

- Plik: maks. 8 MB, typy `image/*` albo `application/pdf` - odrzucane wcześniej, zanim trafi do Claude.
- Limit bezpieczeństwa po stronie serwera: maks. 150 wystąpień łącznie na import, horyzont maks. 12 miesięcy od dzisiaj - liczone na wyniku od modelu, nie ufamy promptowi.
- `czlonek` w odpowiedzi Claude to zamknięty enum: prawdziwe imiona domowników z bieżącego domu plus literał `"Wspólne"` - wymuszony w schemacie narzędzia, nie w promptcie.
- Model: `claude-sonnet-5`. Sekret Edge Function: `ANTHROPIC_API_KEY`.
- Oryginalny plik nie jest nigdzie zapisywany (brak Supabase Storage) - trafia do Claude i jest odrzucany.
- Zapis do bazy dopiero po akceptacji w podglądzie, zawsze z klienta (nie z Edge Function) - respektuje istniejące RLS bez klucza serwisowego.
- Katalog `supabase/` zostaje poza `tsconfig.app.json` (już tak jest - `include: ["src"]`) - pliki Deno nie trafiają do `npm run build`.

---

## Task 1: `zloz` jako współdzielona funkcja czasu

`FormularzWydarzenia.tsx` ma dziś lokalną, nieeksportowaną funkcję `zloz(data, godzina): Date`. Task 5 potrzebuje dokładnie tej samej logiki do złożenia wystąpień z importu AI - przenosimy ją do `czas.ts`, żeby nie duplikować.

**Files:**
- Modify: `src/czas.ts`
- Modify: `src/FormularzWydarzenia.tsx:1-45` (import + usunięcie lokalnej definicji)
- Test: `src/czas.test.ts`

**Interfaces:**
- Produces: `zloz(data: string, godzina: string): Date` eksportowane z `src/czas.ts` - `data` w formacie `'RRRR-MM-DD'`, `godzina` w formacie `'GG:MM'`.

- [ ] **Step 1: Napisz nieprzechodzący test**

Dopisz na końcu `src/czas.test.ts`:

```ts
describe('zloz', () => {
  it('składa datę i godzinę w jedną chwilę czasu lokalnego', () => {
    expect(zloz('2026-09-08', '08:45')).toEqual(new Date(2026, 8, 8, 8, 45))
  })

  it('brakująca godzina/minuta liczy się jako zero', () => {
    expect(zloz('2026-01-01', '')).toEqual(new Date(2026, 0, 1, 0, 0))
  })
})
```

Dodaj `zloz` do istniejącego importu z `'./czas'` na górze pliku (obok `dniWydarzenia`, `minutyOdPolnocy`, ...).

- [ ] **Step 2: Uruchom test - ma failnąć**

Run: `npm test -- czas.test.ts`
Expected: FAIL - `zloz` nie istnieje w `./czas`

- [ ] **Step 3: Przenieś implementację do `czas.ts`**

Dodaj w `src/czas.ts` (obok innych funkcji parsujących, np. po `zTimestampu`):

```ts
/** Skleja datę 'RRRR-MM-DD' i godzinę 'GG:MM' w jedną chwilę czasu lokalnego. */
export function zloz(data: string, godzina: string): Date {
  const [rok, miesiac, dzien] = data.split('-').map(Number)
  const [g, m] = godzina.split(':').map(Number)
  return new Date(rok, miesiac - 1, dzien, g || 0, m || 0)
}
```

W `src/FormularzWydarzenia.tsx` usuń komentarz i lokalną definicję `function zloz(...)` (linie 40-45) i dodaj `zloz` do importu z `'./czas'` w linii 4:

```ts
import { OPISY_POWTARZANIA, godzinaHM, zloz, type Powtarzanie } from './czas'
```

- [ ] **Step 4: Uruchom testy - mają przejść**

Run: `npm test -- czas.test.ts`
Expected: PASS

- [ ] **Step 5: Sprawdź, że reszta aplikacji się kompiluje**

Run: `npx tsc -b`
Expected: brak błędów (FormularzWydarzenia.tsx nadal używa `zloz` tak samo jak wcześniej)

- [ ] **Step 6: Commit**

```bash
git add src/czas.ts src/czas.test.ts src/FormularzWydarzenia.tsx
git commit -m "refactor: przenieś zloz() z FormularzWydarzenia do czas.ts"
```

---

## Task 2: Budowa zapytania do Claude i walidacja wyniku

Czyste funkcje - bez Deno, bez sieci - żeby dało się je testować zwykłym vitestem. To jest serce logiki: enum osób w schemacie narzędzia i limity bezpieczeństwa na wyniku modelu.

**Files:**
- Create: `supabase/functions/_wspolne/importAI.ts`
- Test: `supabase/functions/_wspolne/importAI.test.ts`

**Interfaces:**
- Produces:
  - `type Wystapienie = { data: string; start: string; koniec: string; calodniowe: boolean }`
  - `type Pozycja = { tytul: string; czlonek: string; opis_wzorca: string; wystapienia: Wystapienie[] }`
  - `type ZapytanieWejscie = { prompt?: string; plik?: { dane_base64: string; typ_mime: string } }`
  - `type ZapytanieClaude = { model: string; max_tokens: number; system: string; messages: { role: 'user'; content: Record<string, unknown>[] }[]; tools: Record<string, unknown>[]; tool_choice: { type: 'tool'; name: string } }`
  - `const WSPOLNE = 'Wspólne'`
  - `const MAKS_WYSTAPIEN_LACZNIE = 150`
  - `const MAKS_MIESIECY_HORYZONTU = 12`
  - `budujZapytanie(dzisiaj: Date, domownicy: string[], wejscie: ZapytanieWejscie): ZapytanieClaude`
  - `waliduj(pozycje: Pozycja[], domownicy: string[], dzisiaj: Date): string | null`

- [ ] **Step 1: Napisz nieprzechodzące testy**

Utwórz `supabase/functions/_wspolne/importAI.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { MAKS_WYSTAPIEN_LACZNIE, WSPOLNE, budujZapytanie, waliduj } from './importAI'
import type { Pozycja } from './importAI'

describe('budujZapytanie', () => {
  it('wymusza enum imion domowników plus "Wspólne" w schemacie narzędzia', () => {
    const zapytanie = budujZapytanie(new Date('2026-09-07'), ['Zuzia', 'Ola'], { prompt: 'test' })
    const schemat = zapytanie.tools[0] as {
      input_schema: { properties: { pozycje: { items: { properties: { czlonek: { enum: string[] } } } } } }
    }
    expect(schemat.input_schema.properties.pozycje.items.properties.czlonek.enum).toEqual([
      'Zuzia',
      'Ola',
      WSPOLNE,
    ])
  })

  it('wpisuje dzisiejszą datę do promptu systemowego', () => {
    const zapytanie = budujZapytanie(new Date('2026-09-07'), [], { prompt: 'test' })
    expect(zapytanie.system).toContain('2026-09-07')
  })

  it('dokłada blok obrazu przed tekstem, gdy plik jest zdjęciem', () => {
    const zapytanie = budujZapytanie(new Date('2026-09-07'), [], {
      plik: { dane_base64: 'AAAA', typ_mime: 'image/png' },
    })
    expect(zapytanie.messages[0].content[0]).toMatchObject({ type: 'image' })
  })

  it('plik PDF trafia jako blok "document", nie "image"', () => {
    const zapytanie = budujZapytanie(new Date('2026-09-07'), [], {
      plik: { dane_base64: 'AAAA', typ_mime: 'application/pdf' },
    })
    expect(zapytanie.messages[0].content[0]).toMatchObject({ type: 'document' })
  })

  it('sam prompt, bez pliku, to jeden blok tekstu', () => {
    const zapytanie = budujZapytanie(new Date('2026-09-07'), [], { prompt: 'plan lekcji Zuzi' })
    expect(zapytanie.messages[0].content).toEqual([{ type: 'text', text: 'plan lekcji Zuzi' }])
  })

  it('wymusza narzędzie przez tool_choice', () => {
    const zapytanie = budujZapytanie(new Date('2026-09-07'), [], { prompt: 'test' })
    expect(zapytanie.tool_choice).toEqual({ type: 'tool', name: 'zwroc_pozycje' })
  })
})

describe('waliduj', () => {
  const dzisiaj = new Date('2026-09-07')
  const domownicy = ['Zuzia']

  function pozycja(nadpisz: Partial<Pozycja> = {}): Pozycja {
    return {
      tytul: 'Matematyka',
      czlonek: 'Zuzia',
      opis_wzorca: 'poniedziałki',
      wystapienia: [{ data: '2026-09-08', start: '08:00', koniec: '08:45', calodniowe: false }],
      ...nadpisz,
    }
  }

  it('akceptuje poprawną pozycję', () => {
    expect(waliduj([pozycja()], domownicy, dzisiaj)).toBeNull()
  })

  it('odrzuca nieznaną osobę', () => {
    expect(waliduj([pozycja({ czlonek: 'Ktoś obcy' })], domownicy, dzisiaj)).not.toBeNull()
  })

  it('akceptuje "Wspólne"', () => {
    expect(waliduj([pozycja({ czlonek: WSPOLNE })], domownicy, dzisiaj)).toBeNull()
  })

  it('odrzuca pozycję bez wystąpień', () => {
    expect(waliduj([pozycja({ wystapienia: [] })], domownicy, dzisiaj)).not.toBeNull()
  })

  it('odrzuca termin dalszy niż 12 miesięcy', () => {
    const zaDaleko = pozycja({
      wystapienia: [{ data: '2028-01-01', start: '08:00', koniec: '08:45', calodniowe: false }],
    })
    expect(waliduj([zaDaleko], domownicy, dzisiaj)).not.toBeNull()
  })

  it(`odrzuca więcej niż ${MAKS_WYSTAPIEN_LACZNIE} wystąpień łącznie`, () => {
    const duzo = pozycja({
      wystapienia: Array.from({ length: MAKS_WYSTAPIEN_LACZNIE + 1 }, () => ({
        data: '2026-09-08',
        start: '08:00',
        koniec: '08:45',
        calodniowe: false,
      })),
    })
    expect(waliduj([duzo], domownicy, dzisiaj)).not.toBeNull()
  })

  it('odrzuca wystąpienie, w którym koniec nie jest późniejszy niż początek', () => {
    const zle = pozycja({
      wystapienia: [{ data: '2026-09-08', start: '09:00', koniec: '08:00', calodniowe: false }],
    })
    expect(waliduj([zle], domownicy, dzisiaj)).not.toBeNull()
  })

  it('nie sprawdza godzin przy wydarzeniu całodniowym', () => {
    const calodniowe = pozycja({
      wystapienia: [{ data: '2026-09-08', start: '00:00', koniec: '00:00', calodniowe: true }],
    })
    expect(waliduj([calodniowe], domownicy, dzisiaj)).toBeNull()
  })
})
```

- [ ] **Step 2: Uruchom testy - mają failnąć**

Run: `npm test -- importAI.test.ts`
Expected: FAIL - moduł `./importAI` nie istnieje

- [ ] **Step 3: Napisz implementację**

Utwórz `supabase/functions/_wspolne/importAI.ts`:

```ts
/**
 * Budowa zapytania do Claude i walidacja jego odpowiedzi. Czyste funkcje -
 * bez importu Deno ani klienta Supabase, żeby dało się je testować zwykłym
 * vitestem, tak jak `src/czas.ts` czy `src/notatki.ts`.
 */

export type Wystapienie = {
  data: string // 'RRRR-MM-DD'
  start: string // 'GG:MM' - ignorowane, gdy calodniowe === true
  koniec: string // 'GG:MM' - ignorowane, gdy calodniowe === true
  calodniowe: boolean
}

export type Pozycja = {
  tytul: string
  czlonek: string // dokładnie jedno z podanych imion domowników albo WSPOLNE
  opis_wzorca: string
  wystapienia: Wystapienie[]
}

export type ZapytanieWejscie = {
  prompt?: string
  plik?: { dane_base64: string; typ_mime: string }
}

export type ZapytanieClaude = {
  model: string
  max_tokens: number
  system: string
  messages: { role: 'user'; content: Record<string, unknown>[] }[]
  tools: Record<string, unknown>[]
  tool_choice: { type: 'tool'; name: string }
}

export const WSPOLNE = 'Wspólne'
export const MAKS_WYSTAPIEN_LACZNIE = 150
export const MAKS_MIESIECY_HORYZONTU = 12

const NAZWA_NARZEDZIA = 'zwroc_pozycje'
const MODEL = 'claude-sonnet-5'

function danaDzien(d: Date): string {
  return d.toISOString().slice(0, 10)
}

function schematNarzedzia(domownicy: string[]) {
  return {
    name: NAZWA_NARZEDZIA,
    description:
      'Zwraca listę pozycji kalendarza rozpoznanych z tekstu albo pliku - każda pozycja to jedno ' +
      'powtarzające się albo jednorazowe wydarzenie z konkretnymi wystąpieniami.',
    input_schema: {
      type: 'object',
      properties: {
        pozycje: {
          type: 'array',
          items: {
            type: 'object',
            properties: {
              tytul: { type: 'string' },
              czlonek: { type: 'string', enum: [...domownicy, WSPOLNE] },
              opis_wzorca: {
                type: 'string',
                description:
                  'Krótki, czytelny opis wzorca do pokazania człowiekowi, np. ' +
                  '"poniedziałki 8:00–8:45, do 19 grudnia".',
              },
              wystapienia: {
                type: 'array',
                items: {
                  type: 'object',
                  properties: {
                    data: { type: 'string', description: 'RRRR-MM-DD' },
                    start: { type: 'string', description: 'GG:MM, dowolne przy calodniowe=true' },
                    koniec: { type: 'string', description: 'GG:MM, dowolne przy calodniowe=true' },
                    calodniowe: { type: 'boolean' },
                  },
                  required: ['data', 'start', 'koniec', 'calodniowe'],
                },
              },
            },
            required: ['tytul', 'czlonek', 'opis_wzorca', 'wystapienia'],
          },
        },
      },
      required: ['pozycje'],
    },
  }
}

/**
 * Buduje treść zapytania do Claude Messages API. Czysta funkcja - nie robi
 * żadnego IO.
 */
export function budujZapytanie(
  dzisiaj: Date,
  domownicy: string[],
  wejscie: ZapytanieWejscie,
): ZapytanieClaude {
  const tresc: Record<string, unknown>[] = []

  if (wejscie.plik) {
    tresc.push(
      wejscie.plik.typ_mime === 'application/pdf'
        ? {
            type: 'document',
            source: { type: 'base64', media_type: wejscie.plik.typ_mime, data: wejscie.plik.dane_base64 },
          }
        : {
            type: 'image',
            source: { type: 'base64', media_type: wejscie.plik.typ_mime, data: wejscie.plik.dane_base64 },
          },
    )
  }

  tresc.push({
    type: 'text',
    text: wejscie.prompt?.trim() || 'Rozpoznaj wydarzenia kalendarza z załączonego pliku.',
  })

  return {
    model: MODEL,
    max_tokens: 4096,
    system:
      `Jesteś asystentem kalendarza rodzinnego. Dzisiaj jest ${danaDzien(dzisiaj)}. ` +
      `Domownicy w tym domu: ${domownicy.length > 0 ? domownicy.join(', ') : '(brak - wszystko będzie wspólne)'}. ` +
      'Rozpoznaj z tekstu i/albo pliku listę wydarzeń kalendarza (np. plan lekcji, harmonogram odbioru ' +
      'śmieci, plan zajęć pozalekcyjnych) i zwróć je narzędziem zwroc_pozycje. Zasady: (1) pole "czlonek" ' +
      'musi być dokładnie jednym z podanych imion domowników albo "Wspólne" - nie wolno wpisać innej ' +
      'wartości; (2) jeśli plik/prompt podaje konkretne daty, użyj ich dokładnie; jeśli to wzorzec cykliczny ' +
      'bez podanego końca (np. plan lekcji), przyjmij rozsądny domyślny horyzont, nie dłuższy niż kilka ' +
      'miesięcy; (3) każda pozycja musi mieć co najmniej jedno wystąpienie; (4) jeśli nic sensownego nie da ' +
      'się rozpoznać, zwróć pustą listę pozycji zamiast zgadywać.',
    messages: [{ role: 'user', content: tresc }],
    tools: [schematNarzedzia(domownicy)],
    tool_choice: { type: 'tool', name: NAZWA_NARZEDZIA },
  }
}

/**
 * Waliduje wynik od modelu - to dane z granicy zaufania, sprawdzamy je tak
 * samo jak dane od użytkownika. Zwraca komunikat błędu czytelny dla
 * użytkownika albo `null`, gdy wszystko w porządku.
 */
export function waliduj(pozycje: Pozycja[], domownicy: string[], dzisiaj: Date): string | null {
  const dozwoleniCzlonkowie = new Set([...domownicy, WSPOLNE])
  const limitHoryzontu = new Date(dzisiaj)
  limitHoryzontu.setMonth(limitHoryzontu.getMonth() + MAKS_MIESIECY_HORYZONTU)

  let lacznieWystapien = 0

  for (const pozycja of pozycje) {
    if (!dozwoleniCzlonkowie.has(pozycja.czlonek)) {
      return `Rozpoznano nieznaną osobę: "${pozycja.czlonek}".`
    }
    if (pozycja.wystapienia.length === 0) {
      return `Pozycja "${pozycja.tytul}" nie ma żadnego wystąpienia.`
    }
    for (const w of pozycja.wystapienia) {
      if (new Date(w.data) > limitHoryzontu) {
        return 'Rozpoznano zbyt odległy termin - zawęź zakres albo podziel import.'
      }
      if (!w.calodniowe && w.koniec <= w.start) {
        return `Pozycja "${pozycja.tytul}" ma wystąpienie, w którym koniec nie jest późniejszy niż początek.`
      }
    }
    lacznieWystapien += pozycja.wystapienia.length
  }

  if (lacznieWystapien > MAKS_WYSTAPIEN_LACZNIE) {
    return 'Rozpoznano zbyt dużo terminów - zawęź zakres albo podziel import.'
  }

  return null
}
```

- [ ] **Step 4: Uruchom testy - mają przejść**

Run: `npm test -- importAI.test.ts`
Expected: PASS (13 testów)

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/_wspolne/importAI.ts supabase/functions/_wspolne/importAI.test.ts
git commit -m "feat: budowa zapytania do Claude i walidacja jego odpowiedzi"
```

---

## Task 3: Klient HTTP do Claude API

Jedyne miejsce w tej funkcji, które robi realny `fetch`. Testy mockują `global.fetch`, więc nie potrzebują sieci ani klucza API.

**Files:**
- Create: `supabase/functions/_wspolne/claude.ts`
- Test: `supabase/functions/_wspolne/claude.test.ts`

**Interfaces:**
- Consumes: `type Pozycja`, `type ZapytanieClaude` z `./importAI.ts` (Task 2)
- Produces: `zapytajClaude(zapytanie: ZapytanieClaude, kluczApi: string): Promise<Pozycja[]>`

- [ ] **Step 1: Napisz nieprzechodzące testy**

Utwórz `supabase/functions/_wspolne/claude.test.ts`:

```ts
import { afterEach, describe, expect, it, vi } from 'vitest'
import { zapytajClaude } from './claude'
import type { ZapytanieClaude } from './importAI'

const ZAPYTANIE: ZapytanieClaude = {
  model: 'claude-sonnet-5',
  max_tokens: 100,
  system: 'test',
  messages: [{ role: 'user', content: [{ type: 'text', text: 'test' }] }],
  tools: [],
  tool_choice: { type: 'tool', name: 'zwroc_pozycje' },
}

afterEach(() => {
  vi.unstubAllGlobals()
})

describe('zapytajClaude', () => {
  it('zwraca pozycje z bloku tool_use', async () => {
    const pozycje = [{ tytul: 'Test', czlonek: 'Wspólne', opis_wzorca: '', wystapienia: [] }]
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue({
        ok: true,
        json: async () => ({ content: [{ type: 'tool_use', input: { pozycje } }] }),
      }),
    )

    await expect(zapytajClaude(ZAPYTANIE, 'klucz')).resolves.toEqual(pozycje)
  })

  it('rzuca błędem, gdy API odpowie błędem HTTP', async () => {
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue({
        ok: false,
        status: 401,
        text: async () => 'invalid api key',
      }),
    )

    await expect(zapytajClaude(ZAPYTANIE, 'zly-klucz')).rejects.toThrow('401')
  })

  it('rzuca błędem, gdy odpowiedź nie zawiera wywołania narzędzia', async () => {
    vi.stubGlobal(
      'fetch',
      vi.fn().mockResolvedValue({
        ok: true,
        json: async () => ({ content: [{ type: 'text', text: 'przepraszam, nie rozumiem' }] }),
      }),
    )

    await expect(zapytajClaude(ZAPYTANIE, 'klucz')).rejects.toThrow('narzędzia')
  })
})
```

- [ ] **Step 2: Uruchom testy - mają failnąć**

Run: `npm test -- claude.test.ts`
Expected: FAIL - moduł `./claude` nie istnieje

- [ ] **Step 3: Napisz implementację**

Utwórz `supabase/functions/_wspolne/claude.ts`:

```ts
import type { Pozycja, ZapytanieClaude } from './importAI.ts'

const ANTHROPIC_URL = 'https://api.anthropic.com/v1/messages'
const ANTHROPIC_VERSION = '2023-06-01'

/**
 * Jedyne miejsce z HTTP do Anthropic API. Zapytanie zawsze wymusza narzędzie
 * (patrz `budujZapytanie`), więc odpowiedź to zawsze ustrukturyzowany JSON,
 * nigdy wolny tekst do parsowania.
 */
export async function zapytajClaude(zapytanie: ZapytanieClaude, kluczApi: string): Promise<Pozycja[]> {
  const odpowiedz = await fetch(ANTHROPIC_URL, {
    method: 'POST',
    headers: {
      'content-type': 'application/json',
      'x-api-key': kluczApi,
      'anthropic-version': ANTHROPIC_VERSION,
    },
    body: JSON.stringify(zapytanie),
  })

  if (!odpowiedz.ok) {
    const tekst = await odpowiedz.text()
    throw new Error(`Claude API zwróciło ${odpowiedz.status}: ${tekst}`)
  }

  const dane = await odpowiedz.json()
  const blokNarzedzia = (dane.content ?? []).find(
    (blok: { type: string }) => blok.type === 'tool_use',
  )

  if (!blokNarzedzia) {
    throw new Error('Odpowiedź Claude nie zawiera wywołania narzędzia.')
  }

  return (blokNarzedzia.input as { pozycje: Pozycja[] }).pozycje
}
```

Uwaga: import `'./importAI.ts'` (z rozszerzeniem `.ts`) - tak wymaga Deno w Edge Functions. Vitest i tak rozwiąże ten sam plik co `./importAI` z reszty testów.

- [ ] **Step 4: Uruchom testy - mają przejść**

Run: `npm test -- claude.test.ts`
Expected: PASS (3 testy)

- [ ] **Step 5: Commit**

```bash
git add supabase/functions/_wspolne/claude.ts supabase/functions/_wspolne/claude.test.ts
git commit -m "feat: klient HTTP do Claude API"
```

---

## Task 4: Edge Function `import-ai`

Spina auth, pobranie domowników i oba poprzednie moduły. Kod Deno - nie ma automatycznych testów w tym repo (tak jak żadna inna funkcja Edge nie ma - patrz `docs/superpowers/specs/2026-09-07-poranne-podsumowanie-design.md`), weryfikacja jest ręczna przez `supabase functions serve`.

**Files:**
- Create: `supabase/functions/import-ai/index.ts`
- Modify: `README.md` (sekcja o `ANTHROPIC_API_KEY`)

**Interfaces:**
- Consumes: `budujZapytanie`, `waliduj`, `type ZapytanieWejscie` z `../_wspolne/importAI.ts`; `zapytajClaude` z `../_wspolne/claude.ts`
- Produces: HTTP endpoint `POST /functions/v1/import-ai`
  - wejście: `{ prompt?: string; plik?: { dane_base64: string; typ_mime: string } }`, JWT w `Authorization`
  - wyjście sukcesu: `200 { pozycje: Pozycja[] }`
  - wyjście błędu: `4xx/5xx { blad: string }`

- [ ] **Step 1: Napisz implementację**

Utwórz `supabase/functions/import-ai/index.ts`:

```ts
import { createClient } from 'npm:@supabase/supabase-js@2'
import { budujZapytanie, waliduj, type ZapytanieWejscie } from '../_wspolne/importAI.ts'
import { zapytajClaude } from '../_wspolne/claude.ts'

const MAKS_ROZMIAR_PLIKU = 8 * 1024 * 1024 // bajtów po zdekodowaniu base64
const DOZWOLONE_TYPY = new Set([
  'image/jpeg',
  'image/png',
  'image/webp',
  'image/heic',
  'application/pdf',
])

function bladJson(tekst: string, status: number): Response {
  return new Response(JSON.stringify({ blad: tekst }), {
    status,
    headers: { 'Content-Type': 'application/json' },
  })
}

Deno.serve(async (req) => {
  if (req.method !== 'POST') return bladJson('Tylko POST.', 405)

  let cialo: ZapytanieWejscie
  try {
    cialo = await req.json()
  } catch {
    return bladJson('Nieprawidłowy JSON.', 400)
  }

  if (!cialo.prompt?.trim() && !cialo.plik) {
    return bladJson('Podaj prompt albo plik.', 400)
  }

  if (cialo.plik) {
    if (!DOZWOLONE_TYPY.has(cialo.plik.typ_mime)) {
      return bladJson('Nieobsługiwany typ pliku - zdjęcie albo PDF.', 400)
    }
    // base64 to ~4/3 rozmiaru oryginału - to szacowanie wystarcza do odcięcia nadużyć.
    if (cialo.plik.dane_base64.length > (MAKS_ROZMIAR_PLIKU * 4) / 3) {
      return bladJson('Plik jest za duży (maks. 8 MB).', 400)
    }
  }

  const autoryzacja = req.headers.get('Authorization')
  if (!autoryzacja) return bladJson('Brak autoryzacji.', 401)

  const supabase = createClient(Deno.env.get('SUPABASE_URL')!, Deno.env.get('SUPABASE_ANON_KEY')!, {
    global: { headers: { Authorization: autoryzacja } },
  })

  const { data: uzytkownik, error: bladUzytkownika } = await supabase.auth.getUser()
  if (bladUzytkownika || !uzytkownik.user) return bladJson('Nieprawidłowa sesja.', 401)

  // RLS ("Domownicy - odczyt") ogranicza to zapytanie do domu wywołującego -
  // nie trzeba osobno ustalać household_id.
  const { data: domownicyDb, error: bladDomownikow } = await supabase.from('members').select('name')
  if (bladDomownikow) {
    return bladJson(`Nie udało się wczytać domowników: ${bladDomownikow.message}`, 500)
  }

  const kluczApi = Deno.env.get('ANTHROPIC_API_KEY')
  if (!kluczApi) return bladJson('Brak konfiguracji ANTHROPIC_API_KEY.', 500)

  const domownicy = (domownicyDb ?? []).map((d) => d.name)
  const dzisiaj = new Date()
  const zapytanie = budujZapytanie(dzisiaj, domownicy, cialo)

  let pozycje
  try {
    pozycje = await zapytajClaude(zapytanie, kluczApi)
  } catch (e) {
    return bladJson(`Nie udało się rozpoznać treści: ${(e as Error).message}`, 502)
  }

  const bladWalidacji = waliduj(pozycje, domownicy, dzisiaj)
  if (bladWalidacji) return bladJson(bladWalidacji, 502)

  return new Response(JSON.stringify({ pozycje }), {
    status: 200,
    headers: { 'Content-Type': 'application/json' },
  })
})
```

`SUPABASE_URL` i `SUPABASE_ANON_KEY` Supabase wstrzykuje do każdej Edge Function automatycznie - nie trzeba ich ustawiać ręcznie, tylko `ANTHROPIC_API_KEY`.

- [ ] **Step 2: Dopisz sekcję w README.md**

Dodaj nową sekcję `## Import wydarzeń z AI` w `README.md`, tuż przed `## Skrypty`:

```markdown
## Import wydarzeń z AI

Ekran "Importuj z AI" obok ręcznego dodawania wydarzenia rozpoznaje listę
wydarzeń z wpisanego opisu i/albo wgranego zdjęcia/PDF (np. plan lekcji,
harmonogram odbioru śmieci) przez Claude API i pokazuje podgląd do
zatwierdzenia, zanim cokolwiek trafi do kalendarza - patrz
`docs/superpowers/specs/2026-09-07-import-ai-design.md`.

Wymaga sekretu Edge Function:

\`\`\`bash
supabase secrets set ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx
supabase functions deploy import-ai
\`\`\`

Bez tego sekretu funkcja odpowiada błędem 500 zamiast wywoływać Claude.
```

- [ ] **Step 3: Ręczna weryfikacja lokalnie**

Wymaga zainstalowanego Supabase CLI i `supabase link` do projektu.

```bash
supabase secrets set --env-file supabase/.env.local ANTHROPIC_API_KEY=sk-ant-...
supabase functions serve import-ai --env-file supabase/.env.local
```

W drugim terminalu (podmień `<jwt>` na token zalogowanego użytkownika z devtools przeglądarki, `localStorage` klucz `sb-...-auth-token`):

```bash
curl -i http://localhost:54321/functions/v1/import-ai \
  -H "Authorization: Bearer <jwt>" \
  -H "Content-Type: application/json" \
  -d '{"prompt":"W poniedziałki o 8:00 mam matematykę do 8:45, przez najbliższe 3 tygodnie."}'
```

Sprawdź:
- brak `Authorization` → `401`
- `{}` (bez `prompt` i `plik`) → `400 "Podaj prompt albo plik."`
- `plik.typ_mime: "text/plain"` → `400` o nieobsługiwanym typie
- poprawny prompt → `200` z listą `pozycje`, `czlonek` równy jednemu z Twoich domowników albo `"Wspólne"`

- [ ] **Step 4: Commit**

```bash
git add supabase/functions/import-ai/index.ts README.md
git commit -m "feat: Edge Function import-ai - rozpoznawanie wydarzeń przez Claude"
```

---

## Task 5: Zapis pozycji do bazy (`useWydarzenia.ts`)

Zamienia zaakceptowane pozycje na wiersze `events`/`event_members` - tą samą drogą (i tymi samymi regułami RLS) co ręczne dodawanie wydarzenia.

**Files:**
- Modify: `src/useWydarzenia.ts`
- Test: `src/useWydarzenia.test.ts` (nowy plik)

**Interfaces:**
- Consumes: `zloz`, `naTimestamp`, `nastepnyDzien` z `./czas` (Task 1 dla `zloz`, reszta już istnieje)
- Produces:
  - `type WystapienieImportu = { data: string; start: string; koniec: string; calodniowe: boolean }`
  - `type PozycjaImportu = { tytul: string; czlonekId: string | null; wystapienia: WystapienieImportu[] }`
  - `wierszeZPozycji(pozycja: PozycjaImportu, seriaId: string | null): { title: string; starts_at: string; ends_at: string; all_day: boolean; series_id: string | null }[]` (eksportowane, czyste)
  - `dodajWiele(pozycje: PozycjaImportu[]): Promise<string | null>` - metoda zwracana przez `useWydarzenia()`, `null` = sukces

- [ ] **Step 1: Napisz nieprzechodzący test**

Utwórz `src/useWydarzenia.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { wierszeZPozycji, type PozycjaImportu } from './useWydarzenia'

describe('wierszeZPozycji', () => {
  it('zwykłe wystąpienie dostaje godziny z wystąpienia', () => {
    const pozycja: PozycjaImportu = {
      tytul: 'Matematyka',
      czlonekId: null,
      wystapienia: [{ data: '2026-09-08', start: '08:00', koniec: '08:45', calodniowe: false }],
    }
    expect(wierszeZPozycji(pozycja, null)).toEqual([
      {
        title: 'Matematyka',
        starts_at: '2026-09-08T08:00:00',
        ends_at: '2026-09-08T08:45:00',
        all_day: false,
        series_id: null,
      },
    ])
  })

  it('wystąpienie całodniowe zajmuje dokładnie jedną dobę, koniec wyłączny', () => {
    const pozycja: PozycjaImportu = {
      tytul: 'Wywóz gabarytów',
      czlonekId: null,
      wystapienia: [{ data: '2026-09-15', start: '00:00', koniec: '00:00', calodniowe: true }],
    }
    const [wiersz] = wierszeZPozycji(pozycja, null)
    expect(wiersz.starts_at).toBe('2026-09-15T00:00:00')
    expect(wiersz.ends_at).toBe('2026-09-16T00:00:00')
    expect(wiersz.all_day).toBe(true)
  })

  it('każde wystąpienie dostaje ten sam series_id, jaki podano', () => {
    const pozycja: PozycjaImportu = {
      tytul: 'Matematyka',
      czlonekId: null,
      wystapienia: [
        { data: '2026-09-08', start: '08:00', koniec: '08:45', calodniowe: false },
        { data: '2026-09-15', start: '08:00', koniec: '08:45', calodniowe: false },
      ],
    }
    const wiersze = wierszeZPozycji(pozycja, 'seria-1')
    expect(wiersze.map((w) => w.series_id)).toEqual(['seria-1', 'seria-1'])
  })
})
```

- [ ] **Step 2: Uruchom test - ma failnąć**

Run: `npm test -- useWydarzenia.test.ts`
Expected: FAIL - `wierszeZPozycji` i `PozycjaImportu` nie istnieją w `./useWydarzenia`

- [ ] **Step 3: Zaimplementuj**

W `src/useWydarzenia.ts`:

1. Rozszerz import z `./czas` w linii 3 o `zloz` i `nastepnyDzien`:

```ts
import { naTimestamp, nastepnyDzien, seria, zloz, zTimestampu, type Powtarzanie, type Przedzial } from './czas'
```

2. Dodaj nowe typy obok istniejącego `DaneWydarzenia` (po linii 27):

```ts
/** Jedno wystąpienie z podglądu importu AI. */
export type WystapienieImportu = {
  data: string // 'RRRR-MM-DD'
  start: string // 'GG:MM' - ignorowane, gdy calodniowe === true
  koniec: string // 'GG:MM' - ignorowane, gdy calodniowe === true
  calodniowe: boolean
}

/** Pozycja z podglądu importu AI, po ewentualnej korekcie osoby przez użytkownika. */
export type PozycjaImportu = {
  tytul: string
  /** `null` = wydarzenie wspólne. */
  czlonekId: string | null
  wystapienia: WystapienieImportu[]
}
```

3. Dodaj czystą funkcję konwersji tuż przed `export function useWydarzenia(...)`:

```ts
/**
 * Zamienia jedną pozycję importu na wiersze gotowe do wstawienia do `events`.
 * Wystąpienie całodniowe zajmuje dokładnie jedną dobę - koniec wyłączny, tak
 * jak wszędzie indziej w aplikacji (patrz komentarz przy tabeli `events`).
 */
export function wierszeZPozycji(pozycja: PozycjaImportu, seriaId: string | null) {
  return pozycja.wystapienia.map((w) => {
    if (w.calodniowe) {
      const start = zloz(w.data, '00:00')
      return {
        title: pozycja.tytul,
        starts_at: naTimestamp(start),
        ends_at: naTimestamp(nastepnyDzien(start)),
        all_day: true,
        series_id: seriaId,
      }
    }

    return {
      title: pozycja.tytul,
      starts_at: naTimestamp(zloz(w.data, w.start)),
      ends_at: naTimestamp(zloz(w.data, w.koniec)),
      all_day: false,
      series_id: seriaId,
    }
  })
}
```

4. Wewnątrz `useWydarzenia(...)`, zaraz po definicji `dodaj` (po zamknięciu jej `useCallback`, przed `ustawOsoby`), dodaj:

```ts
/**
 * Zapisuje wiele pozycji naraz - tak jak z importu AI. Każda pozycja dostaje
 * własny `series_id` (albo `null`, gdy ma jedno wystąpienie), więc później da
 * się ją zmienić czy skasować jako całość - dokładnie tak samo jak ręcznie
 * dodaną serię.
 */
const dodajWiele = useCallback(
  async (pozycje: PozycjaImportu[]): Promise<string | null> => {
    for (const pozycja of pozycje) {
      const seriaId = pozycja.wystapienia.length > 1 ? crypto.randomUUID() : null
      const wiersze = wierszeZPozycji(pozycja, seriaId)

      const { data, error } = await supabase.from('events').insert(wiersze).select('id')
      if (error || !data) {
        return `Nie udało się zapisać "${pozycja.tytul}": ${error?.message ?? 'brak odpowiedzi'}`
      }

      if (pozycja.czlonekId) {
        const problem = await przypisz(
          data.map((w) => w.id),
          [pozycja.czlonekId],
        )
        if (problem) {
          return `"${pozycja.tytul}" zapisane, ale nie udało się przypisać osoby: ${problem}`
        }
      }
    }

    await odswiez()
    return null
  },
  [odswiez, przypisz],
)
```

5. Dodaj `dodajWiele` do obiektu zwracanego na końcu `useWydarzenia`:

```ts
return { wydarzenia, ladowanie, dodaj, dodajWiele, zmien, usun }
```

- [ ] **Step 4: Uruchom testy - mają przejść**

Run: `npm test -- useWydarzenia.test.ts`
Expected: PASS (3 testy)

- [ ] **Step 5: Sprawdź całość testów i typów**

Run: `npm test && npx tsc -b`
Expected: wszystkie testy przechodzą, brak błędów typów

- [ ] **Step 6: Commit**

```bash
git add src/useWydarzenia.ts src/useWydarzenia.test.ts
git commit -m "feat: dodajWiele - zapis wielu pozycji z importu AI"
```

---

## Task 6: Komponent `ImportAI.tsx`

Formularz (prompt/plik) → wywołanie Edge Function → podgląd z akceptacją → zapis przez `dodajWiele`. Bez testów automatycznych - w tym repo żaden komponent React nie ma testów (weryfikacja w Tasku 7, ręcznie w przeglądarce).

**Files:**
- Create: `src/ImportAI.tsx`
- Modify: `src/style/formularze.css`

**Interfaces:**
- Consumes: `type PozycjaImportu`, `type WystapienieImportu` z `./useWydarzenia` (Task 5); `type DomownikDb` z `./lib/supabase`; `supabase` z `./lib/supabase`
- Produces: komponent `ImportAI({ domownicy, onZapisz, onZamknij, pokazTytul }: Props)`

- [ ] **Step 1: Utwórz komponent**

Utwórz `src/ImportAI.tsx`:

```tsx
import { useMemo, useState, type ChangeEvent } from 'react'
import { supabase, type DomownikDb } from './lib/supabase'
import type { PozycjaImportu, WystapienieImportu } from './useWydarzenia'

const WSPOLNE = 'Wspólne'
const MAKS_ROZMIAR_PLIKU = 8 * 1024 * 1024

/** Kształt jednej pozycji, jaki zwraca Edge Function `import-ai`. */
type PozycjaAI = {
  tytul: string
  czlonek: string
  opis_wzorca: string
  wystapienia: WystapienieImportu[]
}

/** Pozycja z podglądu, wraz ze stanem edytowanym przez użytkownika. */
type PozycjaWidoku = {
  tytul: string
  opisWzorca: string
  wystapienia: WystapienieImportu[]
  zaznaczona: boolean
  czlonekId: string | null
}

type Props = {
  domownicy: DomownikDb[]
  onZapisz: (pozycje: PozycjaImportu[]) => Promise<string | null>
  onZamknij: () => void
  /** Czy pokazać własny nagłówek - patrz analogiczny prop w FormularzWydarzenia. */
  pokazTytul?: boolean
}

async function plikDoBase64(plik: File): Promise<string> {
  const bufor = await plik.arrayBuffer()
  let binarne = ''
  for (const bajt of new Uint8Array(bufor)) binarne += String.fromCharCode(bajt)
  return btoa(binarne)
}

export function ImportAI({ domownicy, onZapisz, onZamknij, pokazTytul = true }: Props) {
  const [prompt, setPrompt] = useState('')
  const [plik, setPlik] = useState<File | null>(null)
  const [rozpoznawanie, setRozpoznawanie] = useState(false)
  const [bladRozpoznania, setBladRozpoznania] = useState<string | null>(null)
  const [wynik, setWynik] = useState<PozycjaWidoku[] | null>(null)
  const [zapisywanie, setZapisywanie] = useState(false)
  const [bladZapisu, setBladZapisu] = useState<string | null>(null)

  const idPoImieniu = useMemo(() => new Map(domownicy.map((d) => [d.name, d.id])), [domownicy])

  function zmienPlik(e: ChangeEvent<HTMLInputElement>) {
    const wybrany = e.target.files?.[0] ?? null
    if (wybrany && wybrany.size > MAKS_ROZMIAR_PLIKU) {
      setBladRozpoznania('Plik jest za duży (maks. 8 MB).')
      setPlik(null)
      return
    }
    setBladRozpoznania(null)
    setPlik(wybrany)
  }

  async function rozpoznaj() {
    if (!prompt.trim() && !plik) return
    setRozpoznawanie(true)
    setBladRozpoznania(null)

    const cialo: { prompt?: string; plik?: { dane_base64: string; typ_mime: string } } = {}
    if (prompt.trim()) cialo.prompt = prompt.trim()
    if (plik) cialo.plik = { dane_base64: await plikDoBase64(plik), typ_mime: plik.type }

    const { data, error } = await supabase.functions.invoke<{ pozycje: PozycjaAI[] }>('import-ai', {
      body: cialo,
    })

    setRozpoznawanie(false)
    if (error || !data) {
      setBladRozpoznania(`Nie udało się rozpoznać treści: ${error?.message ?? 'brak odpowiedzi'}`)
      return
    }

    setWynik(
      data.pozycje.map((p) => ({
        tytul: p.tytul,
        opisWzorca: p.opis_wzorca,
        wystapienia: p.wystapienia,
        zaznaczona: true,
        czlonekId: p.czlonek === WSPOLNE ? null : (idPoImieniu.get(p.czlonek) ?? null),
      })),
    )
  }

  function przelaczZaznaczenie(i: number) {
    setWynik((stare) => stare!.map((p, idx) => (idx === i ? { ...p, zaznaczona: !p.zaznaczona } : p)))
  }

  function zmienOsobe(i: number, czlonekId: string) {
    setWynik((stare) => stare!.map((p, idx) => (idx === i ? { ...p, czlonekId: czlonekId || null } : p)))
  }

  async function zapisz() {
    if (!wynik) return
    const zaznaczone = wynik.filter((p) => p.zaznaczona)
    if (zaznaczone.length === 0) return

    setZapisywanie(true)
    setBladZapisu(null)
    const problem = await onZapisz(
      zaznaczone.map((p) => ({ tytul: p.tytul, czlonekId: p.czlonekId, wystapienia: p.wystapienia })),
    )
    setZapisywanie(false)
    if (problem) setBladZapisu(problem)
    else onZamknij()
  }

  return (
    <div className="formularz formularz-import-ai">
      {pokazTytul && <h2 className="panel-tytul">Importuj z AI</h2>}

      {!wynik ? (
        <>
          {bladRozpoznania && (
            <p className="blad" role="alert">
              {bladRozpoznania}
            </p>
          )}

          <label htmlFor="import-prompt">Opisz, co dodać</label>
          <textarea
            id="import-prompt"
            value={prompt}
            onChange={(e) => setPrompt(e.target.value)}
            placeholder="np. plan lekcji Zuzi: poniedziałek matematyka 8:00-8:45…"
            rows={4}
          />

          <label htmlFor="import-plik">Albo wgraj plik</label>
          <input id="import-plik" type="file" accept="image/*,application/pdf" onChange={zmienPlik} />
          <span className="wskazowka">Zdjęcie albo PDF, maks. 8 MB.</span>

          <button
            type="button"
            onClick={() => void rozpoznaj()}
            disabled={rozpoznawanie || (!prompt.trim() && !plik)}
          >
            {rozpoznawanie ? 'Rozpoznaję…' : 'Rozpoznaj'}
          </button>

          <button type="button" className="drugi" onClick={onZamknij}>
            Zamknij
          </button>
        </>
      ) : (
        <>
          {bladZapisu && (
            <p className="blad" role="alert">
              {bladZapisu}
            </p>
          )}

          {wynik.length === 0 ? (
            <p className="pusto">Nic nie rozpoznano - spróbuj z innym opisem albo plikiem.</p>
          ) : (
            <ul className="podglad-pozycje">
              {wynik.map((p, i) => (
                <li key={i} className="wiersz-pozycji">
                  <label className="przelacznik">
                    <input type="checkbox" checked={p.zaznaczona} onChange={() => przelaczZaznaczenie(i)} />
                    <span className="wiersz-pozycji-opis">
                      <strong>{p.tytul}</strong> - {p.opisWzorca} ({p.wystapienia.length}{' '}
                      {p.wystapienia.length === 1 ? 'termin' : 'terminów'})
                    </span>
                  </label>
                  <select
                    aria-label={`Osoba dla "${p.tytul}"`}
                    value={p.czlonekId ?? ''}
                    onChange={(e) => zmienOsobe(i, e.target.value)}
                  >
                    <option value="">Wspólne</option>
                    {domownicy.map((d) => (
                      <option key={d.id} value={d.id}>
                        {d.name}
                      </option>
                    ))}
                  </select>
                </li>
              ))}
            </ul>
          )}

          <button
            type="button"
            onClick={() => void zapisz()}
            disabled={zapisywanie || wynik.every((p) => !p.zaznaczona)}
          >
            {zapisywanie ? 'Zapisuję…' : 'Zapisz zaznaczone'}
          </button>

          <button type="button" className="drugi" onClick={() => setWynik(null)}>
            Wróć
          </button>

          <button type="button" className="drugi" onClick={onZamknij}>
            Zamknij
          </button>
        </>
      )}
    </div>
  )
}
```

- [ ] **Step 2: Dodaj style**

Dopisz na końcu `src/style/formularze.css`:

```css
/* --- Podgląd importu AI --- */

.formularz-import-ai textarea {
  padding: 12px;
  border: 1px solid var(--kreska);
  border-radius: 10px;
  background: var(--karta);
  color: var(--tekst);
  font: inherit;
  font-size: 15px;
  resize: vertical;
  margin-bottom: 8px;
}

.formularz-import-ai input[type='file'] {
  margin-bottom: 4px;
}

.podglad-pozycje {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin-bottom: 8px;
}

.wiersz-pozycji {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 10px 12px;
  border: 1px solid var(--kreska);
  border-radius: 10px;
}

.wiersz-pozycji .przelacznik {
  margin: 0;
  align-items: flex-start;
}

.wiersz-pozycji-opis {
  font-size: 14px;
  color: var(--tekst);
}

.przelacznik-importu {
  display: flex;
  gap: 6px;
  margin-bottom: 12px;
}
```

- [ ] **Step 3: Sprawdź typy**

Run: `npx tsc -b`
Expected: brak błędów

- [ ] **Step 4: Commit**

```bash
git add src/ImportAI.tsx src/style/formularze.css
git commit -m "feat: komponent ImportAI - formularz i podgląd"
```

---

## Task 7: Wpięcie w `App.tsx`

Przełącznik "Ręcznie / Importuj z AI" nad panelem dodawania wydarzenia - w obu układach (desktop i telefon). Import widoczny tylko przy dodawaniu nowego wydarzenia, nie przy edycji istniejącego.

**Files:**
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `ImportAI` z `./ImportAI` (Task 6); `dane.dodajWiele` z `useWydarzenia` (Task 5)

- [ ] **Step 1: Dodaj import i stan**

W `src/App.tsx` dodaj import obok `FormularzWydarzenia` (linia 18):

```ts
import { FormularzWydarzenia } from './FormularzWydarzenia'
import { ImportAI } from './ImportAI'
```

Dodaj nowy stan obok `licznikZapisow` (po linii 55):

```ts
const [pokazImportAI, setPokazImportAI] = useState(false)
```

- [ ] **Step 2: Zbuduj przełącznik i panel dodawania**

Zaraz po zamknięciu `const formularzWydarzenia = (...)` (po linii 168, przed `const tresc = (`), dodaj:

```tsx
const przelacznikImportu = !edytowane && (
  <div className="przelacznik-importu" role="group" aria-label="Sposób dodawania">
    <button
      type="button"
      className={`filtr${!pokazImportAI ? ' wlaczony' : ''}`}
      onClick={() => setPokazImportAI(false)}
    >
      Ręcznie
    </button>
    <button
      type="button"
      className={`filtr${pokazImportAI ? ' wlaczony' : ''}`}
      onClick={() => setPokazImportAI(true)}
    >
      Importuj z AI
    </button>
  </div>
)

const panelDodawania =
  edytowane || !pokazImportAI ? (
    formularzWydarzenia
  ) : (
    <ImportAI
      domownicy={osoby.domownicy}
      onZapisz={dane.dodajWiele}
      onZamknij={() => {
        setPokazImportAI(false)
        setDodawanie(null)
      }}
      pokazTytul={!telefon}
    />
  )
```

- [ ] **Step 3: Zresetuj przełącznik przy zamknięciu ręcznego formularza**

W `onZamknij` przekazywanym do `formularzWydarzenia` (linie 162-165) dodaj reset:

```tsx
onZamknij={() => {
  setEdytowane(null)
  setDodawanie(null)
  setPokazImportAI(false)
}}
```

- [ ] **Step 4: Podmień miejsca, gdzie renderuje się `formularzWydarzenia`**

W telefonie (dawna linia 314, wewnątrz `<Arkusz>`):

```tsx
<Arkusz
  otwarty={dodawanie === 'kalendarz' || edytowane !== null}
  tytul={edytowane ? 'Zmień wydarzenie' : pokazImportAI ? 'Importuj z AI' : 'Nowe wydarzenie'}
  onZamknij={() => {
    setDodawanie(null)
    setEdytowane(null)
    setPokazImportAI(false)
  }}
>
  {przelacznikImportu}
  {panelDodawania}
</Arkusz>
```

Na desktopie (dawna linia 327, wewnątrz `<aside className="panel">`):

```tsx
<aside className="panel" aria-label="Szczegóły dnia">
  {widok === 'miesiac' && !edytowane && (
    <ListaDnia dzien={kotwica} wydarzenia={widoczne} osobaPoId={osobaPoId} onKlik={setEdytowane} />
  )}
  {przelacznikImportu}
  {panelDodawania}
</aside>
```

- [ ] **Step 5: Sprawdź typy**

Run: `npx tsc -b`
Expected: brak błędów

- [ ] **Step 6: Ręczna weryfikacja w przeglądarce**

Run: `npm run dev`

1. Zaloguj się, otwórz kalendarz.
2. Kliknij "Importuj z AI" nad formularzem - formularz ręczny znika, pojawia się pole promptu i wyboru pliku.
3. Wpisz prompt (np. "w każdy poniedziałek o 8:00 mam matematykę do 8:45, przez 3 tygodnie") i kliknij "Rozpoznaj" - wymaga wdrożonej funkcji z Tasku 4 i ustawionego `ANTHROPIC_API_KEY`.
4. Sprawdź podgląd: pozycja z liczbą terminów, checkbox zaznaczony domyślnie, rozwijana lista z opcją "Wspólne" i realnymi domownikami.
5. Odznacz jedną pozycję (jeśli jest więcej niż jedna) i kliknij "Zapisz zaznaczone" - po zapisie panel się zamyka, wydarzenia widać w kalendarzu z przypisaną osobą (jeśli wybrana).
6. Kliknij "Ręcznie" - wraca zwykły formularz dodawania.
7. Kliknij istniejące wydarzenie do edycji - przełącznik importu znika, jest tylko formularz edycji.
8. Powtórz kroki 2-6 w widoku telefonu (zwęż okno przeglądarki) - to samo w Arkuszu.

- [ ] **Step 7: Commit**

```bash
git add src/App.tsx
git commit -m "feat: wpięcie importu AI w ekran kalendarza"
```

---

## Self-review (wykonane podczas pisania planu)

- **Pokrycie specu:** wejście prompt/plik (Task 6-7), enum osób (Task 2), limity bezpieczeństwa (Task 2, wywołane w Task 4), brak zapisu pliku (nigdzie nie zapisujemy `plik` do Storage - tylko przekazujemy do Claude i odrzucamy), zapis z klienta po akceptacji (Task 5-6), sekret `ANTHROPIC_API_KEY` (Task 4), model Claude z natywną obsługą PDF/obrazu (Task 2 - blok `document` dla PDF, `image` dla reszty).
- **Placeholdery:** brak - każdy krok ma pełny kod, żadnego "dodaj obsługę błędów" bez treści.
- **Spójność typów:** `PozycjaImportu`/`WystapienieImportu` z Tasku 5 to dokładnie te same nazwy pól używane w Tasku 6 (`ImportAI.tsx`) i Tasku 7 (`dane.dodajWiele`); `Pozycja`/`ZapytanieClaude` z Tasku 2 to te same nazwy, których Task 3 (`claude.ts`) i Task 4 (`index.ts`) używają przy imporcie.
